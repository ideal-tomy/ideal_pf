# ideal_pf：demoを先に掲載し、後から作りこむ進め方

作成日：2026-09-10  
対象：`C:/Users/ryoji/00myapp/ai_demo_workspace/ideal_pf`  
今回の成果物：現状のコードを確認した掲載・実装計画。ページの改修、demo本体の改修、デプロイは実施していない。

## 1. 採用する方針

前回調査の **A「公開してよさそう」4件とB「作りこめば公開可能」24件、計28件を紹介対象**にする。demo本体の完成を待たず、紹介情報を先に掲載し、その後で体験内容を改善する。

紹介ページの掲載と、外部demoを開ける状態は別に管理する。URLがまだないdemoも、説明・画面・現在できることを掲載する。URLのある改善途中のdemoは、できる範囲を説明したうえで「体験版を開く」から案内する。

A/Bは内部の改善優先度に使う。来訪者には評価ランクを見せず、「何を体験できるか」「体験URLがあるか」「現在の制限」を伝える。未完成という理由だけでBを掲載待ちに戻さない。

Cの12件、重複コピー、共通基盤、別プロダクトは今回追加しない。紹介候補28件のうち、先の調査で本番URLのHTTP応答を確認できたものは17件。残る11件は紹介を掲載し、リンクを後から整備する。この17件は全操作の完走確認済みという意味ではない。

根拠：[前回の分類レポート](../catalog/review-2026-09-10/demo-publication-review.md)、[機械可読の分類表](../catalog/review-2026-09-10/classification.json)。公開URLの確認日は同日。今回は外部サイトへの再確認ではなく、ローカルの掲載ページ構造を調査した。

## 2. 現在のページ構造

Vite＋素のJavaScript＋CSSによる静的ページ。React、Next.js、CMS、データベースは使用していない。現状の規模ならこの構成を維持して28件へ拡張できる。

| ファイル | 現在の役割 | 今回の拡充で変更する内容 |
|---|---|---|
| [index.html](index.html) | ヘッダー、業種タイル、4つのタブ、会社・進め方、詳細表示の空コンテナ | 件数・業種の固定値を除去。相談・会社リンクを設定。文言を現状に合わせる |
| [src/data.js](src/data.js) | `ICON`、`SCR`、詳細付き`DEMOS`5件、簡易`OTHERS`5件 | 全28件を同じ形式で管理。URL、表示順、紹介文、体験範囲、画像を登録 |
| [src/main.js](src/main.js) | カード・詳細のHTML生成、タブ切替、絞り込み、履歴、スクロール演出 | 全件の詳細表示、件数集計、状態別CTA、業種操作、詳細URL、任意項目の表示制御 |
| [src/style.css](src/style.css) | 暗色、最大幅420px、固定ヘッダー・下タブ、重なるカード、詳細オーバーレイ | 大カードを少数に限定。28件の一覧、状態表示、実画面画像、キーボード操作の仕上げ |
| [vite.config.js](vite.config.js) | `dist`へのビルド | 原則そのまま |
| [vercel.json](vercel.json) | 全パスを`index.html`へrewrite | クエリ形式の詳細URLなら追加変更不要 |
| [README.md](README.md) | 起動・URL差替え・配信方法 | 追加手順と掲載状態の更新手順に書き換える |

表示は「つくったもの」「業種」「進め方」「ideal」の4タブ。詳細は独立HTMLではなく、`#detail`へ`buildDetail()`の結果を入れる全画面オーバーレイ。カード選択時に`history.pushState({d:1}, '')`を呼ぶが、URLにdemo IDは入らない。

ビルドコマンドは`npm run build`、出力は`dist`。READMEに「mainへpushでVercel本番へ自動反映」とある。ただし今回はVercel側のGit連携設定・本番URL・最新デプロイとの一致は確認していない。

## 3. 件数を増やす前に直す箇所

| 現状の事実 | 影響 | 対応 |
|---|---|---|
| `DEMOS`5件はすべて`url:''` | 詳細を開けても外部demoへ移動できない | 確認済み本番URLを転記する |
| `OTHERS`はID・URL・詳細本文がなく、クリック処理もない | 「そのほか」は押せそうだが進めない | 独立配列をやめ、同じdemoデータから一覧行を作る |
| 件数は`DEMOS.length + OTHERS.length`で10になるが、ラベルは「公開中」 | URLのないものまで稼働中のdemoに見える | 「掲載28件／体験リンク17件」など、掲載数とリンク数を分ける |
| HTML初期値は「公開中12本」、上タイルは4/3/3/2、業種ページは4/3/3/2/2 | 固定値同士と実データが一致しない | 全件数を掲載データから計算する |
| 業種ページの行は`href="#"`だけ | 業種一覧から絞り込めない | 共通の`setCategory()`を呼ぶボタンにする |
| 上タイルのフィルタは、どのタブでもカードの表示だけを変える | 別タブで押すと絞り込み結果が見えない | 一覧タブへ戻す処理とカテゴリ選択を統合する |
| `cat:'all'`は「業種を選ばない」であり、全業種フィルタにも常に混ざる | 件数と結果の意味が曖昧になる | データの`cross-industry`と、UIの「すべて」を区別する |
| カードが全件stickyで、位置が`--i * 9px`ずつ下がる | 28件を大カードにすると後半の位置が252px近くずれ、一覧性も悪い | 大カードは4〜5件、残りは通常の一覧行 |
| 詳細の「ほかのデモ」は自分以外の全件 | 28件だと27件の横スクロールになる | 同業種・関連する3〜4件に限定 |
| `score.toFixed()`や`d.sc.map()`などを無条件に実行する | 点数等のない追加データで描画が失敗する | 評価・制作期間・画像などを任意項目にする |
| `SCR`はCSSによる模式図 | 実際のdemo画面の違いが伝わりにくい | 取得できる実画面を優先。模式図を使う場合は画面イメージと明示 |
| URLがあれば一律で「登録は要りません」 | 認証やAPIキーの条件を誤って説明する可能性 | demoごとの体験条件を表示する |
| 相談・会社サイトは`href="#"` | 興味を持った人の出口がない | 既存の正しい連絡先を確認して設定。未確認の宛先は作らない |
| 詳細はURLで共有できず、popstateでは常に閉じる | 個別紹介を送れず、進む操作で詳細を復元できない | `?demo=<id>`と表示状態を同期する |
| 戻るアイコンに操作名がなく、詳細のフォーカス管理・Esc処理がない | キーボードで詳細を扱いにくい | 操作名、詳細へのフォーカス移動、閉じる操作、元の項目への復帰を追加 |

現状の自己採点、制作日、制作期間、削減効果は紹介用データに直接書かれている。今回のコード調査では数値の根拠を確認できていない。新規28件に架空の点数・工期を埋めて合わせず、根拠のない項目は表示しない。

例えば「毎日1時間の作業がそのままなくなる」「設備と項目は画面から足せる」「月ごとの記録簿を書き出せる」などは、実装との照合が必要。demoで今触れる機能と、導入後に追加する機能を同じ「できること」に混ぜない。

## 4. 既存10項目との対応

| 現在の項目 | 対応先・処理 |
|---|---|
| `construction-record` | `construction_demo`。IDは維持可能。写真→報告書の説明なら`/report`、3体験全体ならトップに合わせる。今回確認済みなのはトップとphoto/report/ops各ルート |
| `inspection-record` | `Inspection_demo`。ID維持。未確認の月次集計・項目追加等の説明は除き、現在の点検入力・記録表示・印刷に合わせる |
| `format-builder` | 前回A/B28件に対応する独立demoを確認できない。初回の掲載から外す。似た用途という理由で別demoを割り当てない |
| `kaigo-record` | 「訪問記録を管理者が見る」という説明は3ロール版に近い。新しい施設向け申し送りと混同しない。3ロール版へ対応付け、現行ソースとURLを確認するまでは紹介のみ |
| `facility-karte` | 広域の自治体施設という説明は`disaster_prevention_demo`へ。単一体育館の`02`版は別カードにする |
| 受入検査の承認 | `Approval_diagram`。詳細付きデータへ移行 |
| 保育の記録と連絡 | rootの`childcare_demo`。archive版は使わない |
| 職人の手配と安全書類 | A/B28件の中に、この内容で確定した独立実装はない。設計資料だけの建設手配を完成demoとして残さない。匠ネットワークを載せる場合は内容・名称とも業者選定へ変更し、別IDにする |
| 導入の費用と回収の試算 | `roi-simulator`は今回のdemo分類の対象外。28件には数えず、必要なら「相談・導入検討」の補助リンクとして扱う |
| 現場のダンドリ表 | `denkigenba_dandori_demo`。詳細付きデータへ移行 |

## 5. 初回の28件とURL

分類は内部管理用。下記の17件は前回調査でHTML応答を確認した本番URL。ハッシュ付きVercelデプロイURLは使わない。既知の未完成部分があるdemoも、紹介・体験範囲を明記して掲載する。

| # | demo／ソース | 内部分類 | 外部リンクの初期値 |
|---:|---|:---:|---|
| 1 | 建設の記録／`construction_demo` | A | https://construction-demo-two.vercel.app/ |
| 2 | カスタマーサポート／`customer_support_demo` | A | https://customer-support-demo-lime.vercel.app/ |
| 3 | シフト管理／`shift_demo` | A | https://shift-demo-ten.vercel.app/ |
| 4 | 製造現場の対応案比較／`axeon_manufacturing` | A | 紹介のみ・URL未確認 |
| 5 | 品質インシデント／`axeon_manufacturing02` | B | https://axeonmanufacturing02.vercel.app/ |
| 6 | 社内ナレッジ／`internal_knowledge_demo` | B | https://internal-knowledge-demo.vercel.app/ |
| 7 | 製造業ナレッジ／`product_flow` | B | https://product-flow-jet.vercel.app/manufacturing |
| 8 | 協力業者選定／`hookapp_demo` | B | https://hookapp-demo.vercel.app/ |
| 9 | AI受付／`assist_demo` | B | 紹介のみ・URL未確認 |
| 10 | 受入検査／`Approval_diagram` | B | https://approval-diagram.vercel.app/ |
| 11 | 設備点検／`Inspection_demo` | B | 紹介のみ・URL未確認 |
| 12 | つけもの手帳／`shikomi_record_demo` | B | https://shikomi-record-demo.vercel.app/ |
| 13 | 保育記録／`childcare_demo` | B | https://ideal-tomy.github.io/childcare_demo/ |
| 14 | 介護申し送り・面談／`kaigo_handoff_demo` | B | https://kaigo-handoff-demo.vercel.app/ |
| 15 | 自治体の防災施設／`disaster_prevention_demo` | B | https://disaster-prevention-demo.vercel.app/ |
| 16 | 総合体育館／`disaster_prevention_demo02` | B | https://disaster-prevention-demo02.vercel.app/ |
| 17 | 申し送りの単体体験／`Care Facility System` | B | https://lambent-smakager-7bcf0a.netlify.app/ |
| 18 | 製造の原因特定／`cause-demo` | B | 紹介のみ・URL未確認 |
| 19 | 電気工事の段取り／`denkigenba_dandori_demo` | B | 紹介のみ・URL未確認 |
| 20 | 経費最適化3業種／`Payments_demo` | B | 紹介のみ・URL未確認 |
| 21 | 薬局間在庫融通／`Pharmacy_transfer_demo` | B | 紹介のみ・URL未確認 |
| 22 | 地域文化／`chiiki-bunka` | B | 紹介のみ・代表シナリオを整理 |
| 23 | 派遣コックピット／`demos/showcase/haken_dash` | B | https://haken-dash.vercel.app |
| 24 | 士業ミニSFA／`demos/showcase/sfa_legal` | B | https://sfa-legal.vercel.app |
| 25 | 人材紹介／`demos/showcase/vietnam_haken` | B | https://vietnam-demo-zeta.vercel.app |
| 26 | 物件マッチング／`demos/showcase/Property Intelligence System` | B | 紹介のみ・URL未確認 |
| 27 | SNS投稿文／`demos/prototypes/sns_post_generator` | B | 紹介のみ・URL未確認 |
| 28 | ケア記録3ロール／`demos/archive/legacy-demo/AXEON_demo/kaigo_demo` | B | 紹介のみ。過去URLは下記参照 |

28番には8/18の記録で`https://kaigo-operation-demo.vercel.app/`があるが、9/10のHTTP確認対象には含まれていない。ローカルのarchive内コピーが本番の正本かも未確定なので、まず紹介のみとし、確認後にリンクを追加する。

介護の14・17・28は別の体験として説明する。17は「短い申し送り体験」、14は「申し送り・面談・日報」、28は「現場・内勤・医療連携」。完全同一の`候補/voice_memo`は追加しない。

## 6. ページの見せ方

### 一覧

暗色・スマートフォン中心・下タブの見た目を維持する。全面的なデザイン変更を先行掲載の条件にしない。

- 上部：会社名、「掲載28件」、必要なら小さく「体験リンク17件」。
- おすすめ：既存の大カードを5件。建設、カスタマーサポート、シフト、品質インシデント、介護申し送りを初期候補にする。Bも選べる。
- そのほかのデモ：残る23件を通常の一覧行。すべて詳細を開ける。
- 絞り込み：タイトル・用途を対象に検索し、業種でも探せる。絞り込み中は28件を重複のない通常一覧で表示すると分かりやすい。
- URL未確認の項目は「紹介のみ」。URLがある項目は「体験版あり」。B全件に大きな「未完成」ラベルを付ける必要はない。

件数は同じ掲載配列から計算する。将来、体験URLが停止した場合は紹介を残したままリンク数を減らす。

### 業種

現在の4タイルに全カテゴリを押し込まない。上部は建設・製造・介護保育・自治体のショートカットとして残し、「業種」タブには全カテゴリを表示する。

推奨する主カテゴリは、建設・現場／製造・設備／医療・介護・保育・薬局／自治体・地域／飲食・食品／人材・士業／不動産／業種を選ばない。初回は1件につき主カテゴリ1つとし、シフト＝飲食、つけもの＝食品、地域文化＝自治体・地域などに割り当てる。横展開できる業種はタグで補足する。

「すべて」はフィルタ解除用のUI値。「業種を選ばない」は実際のカテゴリ。件数は主カテゴリ所属数を表示し、他カテゴリを暗黙に混ぜない。

### 詳細

表示順は、名称・用途 → 現在の体験条件とCTA → 実画面 → どんな場面で役立つか → この体験版でできること → 今後追加すること → 関連demo・相談、とする。

具体例：介護申し送りなら「時刻ごとのメモをまとめ、確認して記録する体験です」。固定音声シナリオなら「録音部分は用意された内容で進みます」。受入検査なら「図面と証明書の照合、保留、承認の画面構成を確認できます。記録・出力の操作は今後追加します」。

「そのまま使えるか」という自己採点は一旦外す。改善途中でも体験できるものを先に紹介する方針と、実運用できるかの採点を混在させない。制作期間・公開日も確認できるものだけ表示する。

## 7. データの変更案

`DEMOS`と`OTHERS`を単一の`DEMOS`に統合する。おすすめは`featured`または`featuredOrder`で選び、簡易一覧と詳細で同じオブジェクトを参照する。

| フィールド案 | 用途 |
|---|---|
| `id` | URL・詳細表示に使う固定ID。名前変更でも変えない |
| `listed` | 紹介に掲載するか。初回A/B28件はtrue |
| `featuredOrder` | おすすめの並び順。未指定は通常一覧 |
| `category`、`tags` | 主カテゴリと横展開先・機能タグ |
| `title`、`lead`、`audience` | 名称、一文の用途、使う人 |
| `url`、`linkState` | 本番URLと`available / preparing / paused` |
| `experienceNote` | 固定サンプル、録音演出、接続条件など来訪者に必要な説明 |
| `can`、`planned` | 今の体験でできることと、今後追加すること |
| `screenshots` | 実画面のパス・代替テキスト・キャプション。空配列も許可 |
| `relatedIds` | 関連する3〜4件まで |
| `lastLinkCheckedAt` | URL応答を確認した日。機能完走確認日と混同しない |

内部のA/B、ローカルパス、細かな不具合、作業担当・優先度は、後述の改善管理MDに置く。公開用JSはブラウザへ配信されるので、内部メモの置き場にしない。

このデータ構造は移行案であり、現行の`cat`、`plain`、`cls`等がそのまま動くわけではない。`main.js`の参照と同時に変更する。短期実装では`cls`と`icon`を残し、表示名はテキストとして扱う。外部由来の文字列を未処理で`innerHTML`へ連結しない。

分類JSONを実行時にワークスペース外から読む構成にはしない。初回転記の材料として使い、Viteがビルドできる公開用データを`ideal_pf`内に持つ。画像も兄弟フォルダへのパスで参照せず、`public/images/demos/<id>/`へ配置する。

### CTAの規則

| 状態 | 表示と操作 |
|---|---|
| `available`＋有効なhttps URL | 「体験版を開く」。別タブ。demo別の体験条件を表示 |
| `preparing` | 紹介詳細は表示。「体験URLを準備しています」。相談先が確定していれば相談リンク |
| `paused` | 紹介詳細を残し「体験版は調整中です」。外部リンクを止める |
| URLなし・不正形式・ログイン画面へ転送 | activeな外部CTAを出さない。内部管理へ確認事項を残す |

既知のUIの粗さは`available`を妨げない。関係ないページや認証画面へ送るリンクは掲載せず、紹介のみで進める。

## 8. 実装する順序

### 第1段階：紹介ページ側を28件対応にする

1. 上記28件を固定IDで単一データへ登録。既存の仮項目を対応表に沿って置換・除外する。
2. `score`・制作期間等の必須依存を外し、情報がなくても詳細が開くようにする。
3. 大カード5件＋通常一覧23件へ変更し、一覧行にも詳細へのクリックを接続する。
4. 掲載数・リンク数・カテゴリ数を同じデータから集計する。
5. 上タイルと業種タブのフィルタを共通化し、すべて／検索／0件表示を整える。
6. 17件の確認済みURLを設定し、11件は紹介のみで表示する。
7. 既存の紹介文を現実装に合わせて修正。未確認の数字・未実装機能の断定・一律の登録不要文言を外す。
8. `?demo=<id>`の読込・戻る・進む・再読込、フォーカスとEscでの閉じる操作を整える。
9. 正しい会社・相談リンクを設定する。未確認の場合はリンクを捏造せず、確定した案内だけ出す。

この段階ではdemo本体のB改善を待たない。体験範囲の説明が書けるものから紹介を揃える。

### 第2段階：掲載前の表示確認と配信

`npm run build`で生成を確認し、プレビューで下記を確認する。README記載どおりmainへのpushで本番に反映される可能性があるため、実装差分・プレビュー・掲載内容を確認してから反映する。現時点の依頼は計画MDの作成までであり、本書はpushや公開操作の実施記録ではない。

- 全28件が重複なく表示され、すべて詳細が開く。
- 初期値では掲載28件・体験リンク17件、カテゴリの合計は28件。後からURL確認結果が変われば件数も追従する。
- 詳細に未設定の数字・`undefined`・壊れた画像・押しても反応しない項目がない。
- 17件の外部リンクは本番URLを使う。配信直前にはリンク先と体験条件を再確認する。
- 紹介のみ11件に空リンクや無効な「開く」ボタンを残さない。
- おすすめと残りの一覧、カテゴリ検索、0件、解除、各タブからの業種選択が動く。
- 個別URLを新しいタブで開ける。戻る・進む・再読込で想定した詳細が表示される。不明IDは一覧へ戻す。
- 390px・768px・1440px、キーボード、動きを減らす設定で操作する。上帯・下タブ・カードが操作対象を隠さない。
- 自己採点や工期を省略しても余白が不自然にならない。

検索・件数・ID重複・CTA分岐のデータ整合性は軽い検証にする。大量のUI単体テスト作成より、一覧→詳細→外部demo／相談の一連の動作を優先する。

### 第3段階：掲載後、demo本体を順番に改善する

改善は公開ページとは別の作業として管理し、完成したものから説明・画像・URL状態を更新する。

| 優先 | 対象 | 最初に直す内容 |
|---|---|---|
| 1 | `axeon_manufacturing02` | ナビ名と遷移画面のずれ。クラッシュの指摘ではない。サイドナビを含む完走を確認 |
| 1 | `internal_knowledge_demo` | 最初のガイド質問から根拠付き回答へ到達する流れ |
| 1 | `shift_demo` | 8/18に記録されたROIリンク不備の再確認・修正 |
| 1 | `Approval_diagram` | 無効な記録・出力操作を実装するか、画面紹介として明瞭にする |
| 1 | `kaigo_handoff_demo`、`childcare_demo`、`shikomi_record_demo` | 固定シナリオ・録音演出の説明と、記録・確認・出力の体験完走 |
| 2 | `axeon_manufacturing`、`Inspection_demo`、`assist_demo` | 公開URLを用意し、紹介のみから体験版ありへ切替 |
| 2 | 防災2種類 | 判断訂正、保存範囲、一覧・履歴の整合、スマホ操作 |
| 2 | `product_flow` | ブランド・掲載エイリアス、タイトル、回答後の次の導線 |
| 3 | その他のURL未確認demo | 代表版選定→短い操作の完走→公開URL追加 |
| 3 | 派遣・士業・人材紹介・不動産 | 入力から結果までの短い体験、保存・初期化・スマホの仕上げ |

これは初期優先度。公開後に反応のある業種・相談の多いdemoへ順序を変える。API利用がある建設・SNS生成等では、実データを使う導線を広げる段階で利用上限と失敗時の挙動を確認する。

## 9. 後から更新しやすい管理

実装時に`ideal_pf/docs/demo-improvements.md`を用意し、公開用データのIDと同じIDで次を管理する。改善項目の初期値は前回分類表の`next`から転記する。

| demo ID | 内部分類 | 紹介掲載 | URL状態 | 次の改善1〜3件 | 優先 | 最終確認日 |
|---|---|---|---|---|---|---|
| quality-incident | B | 掲載 | available | ナビ対応・提出操作・スマホ完走 | 1 | 2026-09-10（URL応答） |
| inspection-record | B | 掲載 | preparing | 記録と印刷の完走・公開URL | 2 | 2026-09-10（コード） |

更新は「demo本体を改善 → 体験確認 → 公開用の`can`・`planned`・画像・リンク状態を更新 → 紹介ページをビルド・反映」の順。demoの改善ごとにカードを作り直したり、Aになった時点で新しいカードを追加したりしない。掲載IDを維持し、同じ詳細ページを育てる。

実画面をまだ撮れていない項目は、模式図または説明だけで先に掲載できる。実画面の撮影は既存の保存済み画像を確認できるものから進め、全28件の撮影完了を初回掲載の条件にしない。CMS導入・フレームワーク変更・全demoの再開発も初回掲載には不要。
